// includes method on string & array
const sentence = 'hello everyone welcome to bootcamp course';

console.log(sentence.includes('bootcamp'));

const words = ['a', 'b', 'c', 'd', 1, 2, 3];

console.log(words.includes(10));

// power x**2
const expo = (x) => x ** 2;
const volume = (v) => v ** 3;
